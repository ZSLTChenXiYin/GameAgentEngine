# GameAgentTestWorker

`GameAgentTestWorker` is the legacy compatibility wrapper for `GameAgentWorker`.

New development, packaging, and documentation should use [`cmd/gameagentworker/README.md`](../gameagentworker/README.md) and the `GameAgentWorker` executable name.

This command currently remains available so existing integration scripts can keep working during migration.
